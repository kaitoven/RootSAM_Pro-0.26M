# =================================================================
# RootSAM-Pro 官方环境依赖清单 (requirements.txt)
# Python 版本: 3.12
# 宿主机环境: AutoDL (Torch 2.8.0+cu128)
# =================================================================

# -----------------------------------------------------------------
# 1. 深度学习核心底座 (🚨 AutoDL 镜像已自带，请务必保持注释，严防覆盖本地加速环境)
# -----------------------------------------------------------------
# torch==2.8.0+cu128
# torchvision==0.23.0+cu128

# -----------------------------------------------------------------
# 2. 物理形态学与张量引擎 (ZCMP 零成本探头 与 SDF 豁免场强依赖)
# 🚨 极其关键：限定 numpy < 2.0，彻底防止 Scipy/Skimage 的底层 C 接口断裂崩溃
# -----------------------------------------------------------------
numpy>=1.26.4,<2.0.0
scipy>=1.12.0
scikit-image>=0.22.0

# -----------------------------------------------------------------
# 3. 图像 I/O 与视觉流形处理
# 🚨 强制使用 headless 版本，杜绝 AutoDL 服务器缺少 libGL.so.1 报错
# -----------------------------------------------------------------
opencv-python-headless>=4.9.0.80

# -----------------------------------------------------------------
# 4. 实验监控系统与顶刊图表渲染
# -----------------------------------------------------------------
tensorboard>=2.16.2
matplotlib>=3.8.3
pillow>=10.2.0
pandas>=2.0.0
pyyaml>=6.0.1

# -----------------------------------------------------------------
# 5. Meta SAM 2.1 官方底层隐式依赖 (防漏补齐)
# -----------------------------------------------------------------
hydra-core>=1.3.2
iopath>=0.1.10
tqdm>=4.66.0

# =================================================================
# 🚨 架构师的部署备忘录 (Deployment Memo)
# =================================================================
# SAM 2.1 绝不能在此处直接 pip 安装，它包含需要动态编译的 CUDA Fused 算子。
# 请在运行完 pip install -r requirements.txt 后，手动执行：
# git clone https://github.com/facebookresearch/sam2.git
# cd sam2 && pip install -e . && cd ..
# =================================================================
